For a required independent review, dispatch a new Codex subagent with a bounded review task and no inherited author conversation. If the spawn interface offers `fork_turns`, set it to `none`. Supply the absolute checkout path, diff base, acceptance criteria, evidence locations, and the adversarial brief above. Have it read `{{aiwf:skills_dir}}/wf-review-code/SKILL.md` and, when the design lens applies, `{{aiwf:skills_dir}}/wf-rethink/SKILL.md`. Request findings with file locations and a verdict, then wait for its result before presenting the review gate. Use the available general-purpose subagent; no custom reviewer role file is required. If delegation or fresh-context isolation is unavailable, report the limitation and request a separate fresh reviewer session. Do not replace required independent review with the author's self-review.
